import React from 'react'
import Canton from '@src/assets/icon/canton.svg'
import {IconProps} from '@src/types/declare'

const CantonIcon = (props: IconProps) => {
  return <Canton width={props.width} height={props.height} fill={props.fill} />
}

export default CantonIcon
