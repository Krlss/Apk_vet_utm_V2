import React from 'react'
import Parish from '@src/assets/icon/parish.svg'
import {IconProps} from '@src/types/declare'

const ParishIcon = (props: IconProps) => {
  return <Parish width={props.width} height={props.height} fill={props.fill} />
}

export default ParishIcon
