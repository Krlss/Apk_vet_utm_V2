import React from 'react'
import Intersex from '@src/assets/icon/intersex.svg'
import {IconProps} from '@src/types/declare'

const IntersexIcon = (props: IconProps) => {
  return (
    <Intersex width={props.width} height={props.height} fill={props.fill} />
  )
}

export default IntersexIcon
