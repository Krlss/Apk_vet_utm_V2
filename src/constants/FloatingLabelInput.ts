import AppStyles from '@src/themes/AppStyles';

/** For text label of input floating with label, position top */
export const textPositionMove = AppStyles.inputHeight.default / 2 - (AppStyles.font.size.default / 1.5);
/** For text label of input floating with label, position middle */
export const defaultPositionText = -(AppStyles.font.size.default / 1.5);